import { CostCenter } from '../../components/CostCenter';

/** Static route takes precedence over the generic protected-section placeholder. */
export default function Costs() {
  return <CostCenter />;
}
