import { BugCenter } from '../../../components/BugCenter';

/** Explicit route: it takes precedence over the generic protected section. */
export default function BugsRoute() {
  return <BugCenter />;
}
